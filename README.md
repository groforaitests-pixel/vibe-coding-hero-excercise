# vibe-coding-hero-excercise

**Список метрик для цифрового продукта B2C.** Справочник из 13 KPI, сгруппированных по трём блокам: клиентский опыт, операционная эффективность и монетизация. Описывает формулы, владельцев метрик, источники событий и рекомендуемые связки метрик «читать вместе» для корректной интерпретации.

## Что внутри

- **`index.html`** — самодостаточная страница дашборда с оглавлением и якорями к каждой метрике. Определяет design-токены (`--color-*`, `--font-sans`, `--border-radius-*`) с поддержкой светлой и тёмной темы через `prefers-color-scheme`. Открывается в любом браузере без сборки.
- **`b2c-digital-product-metrics.html`** — исходный HTML-фрагмент без `<html>/<head>/<body>`, предназначенный для встраивания в окружение, где design-токены уже определены (например, Claude-артефакт). Переименован с исходного `GRO_metrics_тренажеры_v1_31032026 (1).html`.

## Как посмотреть

```bash
# macOS
open index.html
# Linux
xdg-open index.html
```

## Группы метрик

| Группа | Метрики |
|---|---|
| Клиентский опыт | Session Start Rate, Drop-off, Session Duration, Artifact Save Rate, Completion Rate, Rating, Energy Score, Return Rate |
| Операционные | Token Usage + Cost per Session, Error Rate, Forced Exit Rate |
| Монетизация | Next Trainer Conversion Rate, Partner Click-through Rate |

Блок «Читать вместе» описывает ключевые связки метрик для интерпретации.

## Предложенные улучшения

Анализ проекта выявил ряд возможных улучшений — в этой ветке реализованы пункты 1, 3, 4, 5:

1. **README с описанием проекта** ✅ — добавлено описание «Список метрик для цифрового продукта B2C».
2. **Standalone-страница** ✅ — `index.html` определяет design-токены, которые исходный фрагмент требовал, но не объявлял, и оборачивает разметку в полный HTML-документ с light/dark-темой.
3. **Понятное имя файла** ✅ — фрагмент переименован из `GRO_metrics_тренажеры_v1_31032026 (1).html` в `b2c-digital-product-metrics.html` (ASCII, без пробелов).
4. **Оглавление с якорями** ✅ — в `index.html` добавлен блок TOC с ссылками на каждую из 13 метрик и блок «Читать вместе».
5. **Возможные следующие шаги** — добавить `.gitignore`, вынести CSS в отдельный файл, собрать единый источник истины в JSON/YAML и генерировать HTML из него, чтобы не дублировать контент между `index.html` и фрагментом.
